import mongoose from 'mongoose';
import dotenv from 'dotenv';

//Meteremos PORT, HOST y demases cosas de Mongo connection - Lo que haremos sera establecer la conexion para poder usar las busquedas que tenemos definidas dentro de User.orm.ts
